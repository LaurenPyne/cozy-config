#!/usr/bin/env sh

set -eu

PROJECT_DIR="${1:-$PWD}"

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"

. "$SCRIPT_DIR/lib/bootstrap.sh"

openclaw_profile

launch /run/current-system/sw/bin/bash
